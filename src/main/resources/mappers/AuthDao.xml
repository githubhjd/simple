<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springboot.simple.dao.SysAuthDao">

    <select id="querySysAuthList" resultType="SysAuth">
        select * from sys_auth
    </select>

    <delete id="deleteOneSysAuthById">
        delete from sys_auth where id = #{id}
    </delete>

</mapper>